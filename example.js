function quadratic(a, b, c) {
    const topEquation = b**2 -4*a*c;
    const x1 = (-b + math.sqrt.(topEquation)) / (2 * a);
    const x2 = (-b - math.sqrt.(topEquation)) / (2 * a);
    console.log(x1, x2);
}
quadratic(3, 4 , 6)

function equation(a, b, d) {
    const upEquation =  b**2 -4*a*d;
    const x3 = (-b + math.sqrt(upEquation)) / (2*a);
    const x4 = (-b + math.sqrt(upEquation)) / (2*a);
    console.log(x3,x4);




}

equation( 1, 2, 3);